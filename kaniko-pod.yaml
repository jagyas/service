apiVersion: v1
kind: Pod
metadata:
  name: kaniko
spec:
  containers:
  - name: kaniko
    image: gcr.io/kaniko-project/executor:latest 
    args: ["--context=git://github.com/jagyas/service:0.0.1",
            "--destination=jagyas/service",
            "--cache=true",
            "--cache-copy-layers",
            "--cache-repo=jagyas/cache"]
    volumeMounts:
    - name: kaniko-cache
      mountPath: /cache
    - name: docker-config
      mountPath: /kaniko/.docker/
  restartPolicy: Never
  volumes:
  - name: docker-config
    secret:
      secretName: regcred
      items:
          - key: .dockerconfigjson
            path: config.json
  - name: kaniko-cache
    persistentVolumeClaim:
            claimName: jenkins-pv-claim
